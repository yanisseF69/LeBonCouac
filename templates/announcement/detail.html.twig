{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>{{ announcement.title }}</h1>
        <h2> {{ announcement.price }} </h2>
        <p> {{announcement.description }} </p>
    </div>
{% endblock %}